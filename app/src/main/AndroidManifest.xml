<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.financemanager">

    <application
        android:allowBackup="true"
        android:icon="@drawable/alkalmazas_ikon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinanceManager">
        <activity
            android:name=".Tranzakciok.UpdateActivity"
            android:label="Manage transaction"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Tranzakciok.AddActivity"
            android:label="Add transaction"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.FinanceManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>